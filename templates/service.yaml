apiVersion: v1
kind: Service
metadata:
  name: {{ include "week20app.fullname" . }}
  labels:
    {{- include "week20app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "week20app.selectorLabels" . | nindent 4 }}
